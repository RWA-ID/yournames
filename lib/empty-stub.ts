/**
 * Empty module used to satisfy optional dependencies that Turbopack can't
 * resolve (wagmi's tempo connector optionally imports an "accounts" package
 * we don't use; WalletConnect's pino optionally wants pino-pretty).
 */
export {};
